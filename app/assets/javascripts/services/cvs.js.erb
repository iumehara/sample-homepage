var cvServices = angular.module('cvServices', []);

cvServices.factory('cvData', ['$http',
	function($http) {
		cvData = {
			data: {
				cvs: null
			}
		}

		cvData.loadData = function() {
			$http.get('../assets/data-ae64ef630ce732689d83f66d827a6beb.json').
				success(function(data) {
					cvData.data.cvs = data;
					console.log("Successfully loaded posts");
				}).
				error(function(data) {
					console.error('Failed to load posts.');
				});			
		}

		return cvData;
	}
]);